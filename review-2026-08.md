# EdgeStream 项目设计与实现深度评审报告

日期：2026-08-04
基线：`go build`、`go vet`、`go test ./...`、`go test -race` 全部通过；14 个包无测试文件。

## 评审范围与方法

五条独立轴线并行评审：

1. **架构与设计一致性** — 设计文档（`edgestream-design.md` 等）vs `internal/` 全部实现
2. **引擎并发正确性** — `engine`/`topology`/`buffer`(WAL)/`message` 的竞态、泄漏、消息语义
3. **配置与 EQL** — `internal/config`（YAML/HOCON）、`internal/eql`（CEL 封装）
4. **插件实现质量** — source/sink/transform/codec 全部 12 个插件
5. **测试与工程化** — 测试文件、observability、CLI、Makefile、示例

## 核心结论

骨架优秀（`stage`/`codec`/`registry` 接口是合格的深模块，Message 的 ackFn/refCount/COW 模型对齐 Benthos 成熟做法，topology 校验完整），但可靠性语义停留在纸面，三个系统性问题贯穿各轴线。

---

## 主题一：at-least-once 叙事不成立（最严重）

- 🔴 **WAL 先推进 offset 后投递 = at-most-once**：`internal/buffer/disk.go:132` 解码成功即持久化 offset，崩溃即永久丢消息；WAL 无 CRC，torn write 后该 edge 永久停摆无法自愈（`wal.go` / `edge.go:175-178`）
- 🔴 **停机/热重载最后一批必丢**：`internal/engine/pipeline.go:616` 用已取消的 ctx 做最终 flush，sink 写入几乎必败；writer goroutine 退出竞态导致 writeCh 残留 job 永不 Ack（`pipeline.go:565-577`）
- 🔴 **required 边 DSL 求值失败导致双重 Ack**：`pipeline.go:448-451` + `dispatch.go:407-409`，AckFn 无幂等保护（`internal/message/message.go:64`），inflight 计数可变负（`inflight.go:44`）
- 🔴 **COW 被 map/route transform 实质破坏**：`plugins/transform/map/map.go:41`、`plugins/transform/route/route.go:94` 未调 `EnsureWritable()`，fan-out 分支共享同一 `parsedData` map 互相串扰（三轴独立发现）
- 🔴 **`consumer.offset` 非原子写可致 pipeline 无法启动**：`disk.go:362-366` 直接截断写、无 fsync、无 tmp+rename
- 🟡 **Enqueue/Close 竞态可 panic**：`edge.go:97-103` 锁外发送，`Close` 可在窗口期 `close(e.mem)`
- 🟡 **reload 失败留下死管道**：`reload.go:256-268` 旧管道已停不可重启
- 🟡 **停机时内存残留消息从不 Ack**：`edge.go:215-223`、`pipeline.go:463-467`
- 🟡 **goroutine 泄漏面**：`pipeline.go:319-321` `src.Consume` 不属于任何 WaitGroup；`reload.go:148,190` 用 `context.Background()`
- 🟡 **overflow 模式乱序**：`edge.go:174-184` drainLoop 优先读磁盘，后溢出消息插队
- 🟡 **transform 输出路由静默丢消息**：`dispatch.go:59-61` 新 ID 无匹配时 `continue`；同 ID 多输出可能双 Ack 或永不 Ack

## 主题二：文档口径互相打架且与代码脱节

- 🔴 **WASM 是空壳**：`plugins/transform/wasm/wasm.go:12` 返回 "not implemented"，README 特性表却当作已有能力；设计 §9.8 与 §12 自相矛盾
- 🔴 **边级 DLQ 完全未实现**：`pipeline.go:702 deliverToDLQ` 只读 pipeline 级 DLQ；`_examples/06-edge-delivery.yaml` 承诺的边级语义不生效；DLQ metadata 缺 `er-error-type`/`er-original-source`；`dlqSink.Write` 用 `context.Background()` 且吞错
- 🔴 **`validate` 不做 §8.7 承诺的校验**：`topology/validate.go` 不查 DSL 编译、CEL TypeCheck、组件 type 注册，推迟到 run 期
- 🟡 **可观测性严重缩水**：`internal/observability/metrics.go` 仅 9 个指标 vs 设计 §10.2 的 30+；`trace.go` 是 noop；README 声称的 OTLP/notifications/K8s Operator 均无代码
- 🟡 **Codec 体系被 transform 架空**：`map.go:51-57`/`route.go:101` 硬编码 `json.Unmarshal/Marshal`，之后引擎 `reserializeIfDirty`（`pipeline.go:379`）再编一次——双重序列化，非 JSON codec 下错误
- 🟡 **热加载顺序与设计相反**：`reload.go:255-264` 先 `old.Stop()` 再 `NewPipeline`，插件实例化失败时旧管道已停
- 🟡 **配置字段静默失效**：`delivery.timeout`、`batch.max_bytes`、`buffer.key` 解析后无人使用
- 🟡 **README 超前、设计 §12 滞后**：热加载已实现仍标 `[ ]`；`PollingSource` 接口无调用点（死抽象）；`instantiateStage` 注入 7 个无人读取的内部配置键

## 主题三：静默失败是默认错误策略

- 🔴 **YAML 未知字段静默忽略**：`internal/config/loader.go:64` `KnownFields(false)`
- 🔴 **HOCON 类型错误静默回零**：`internal/config/hocon.go:449-490`，`max_workers = "abc"` → 0；`workers.go:39-44` transform 可分到 0 worker，消息永久滞留
- 🔴 **官方示例 `_examples/08-hocon-linear.conf` 无法通过 `validate`**（前两行中文注释导致 hocon 库解析为空）——两轴独立复现
- 🔴 **`raw` codec Encode 对不支持类型返回 `nil, nil`**：`plugins/codec/raw/raw.go:30`，下游无法区分空数据与编码错误
- 🔴 **http sink 无重试、单条失败跳过整批、timeout 配置未生效**：`plugins/sink/http/http.go:30,42-57`
- 🔴 **cron 背压静默丢 tick**：`plugins/source/cron/cron.go:117`；**httpserver 启动失败不可见 + 无 `MaxBytesReader`**：`httpserver.go:56,69`
- 🔴 **kafka source pending map 内存泄漏 + Commit 用不可取消 context**：`plugins/source/kafka/kafka.go:95-97,119`
- 🟡 **环境变量替换范围不完整**：`loader.go:99-113` 不覆盖 `engine`/`observability`/`edgeDefaults`/`codecs`，且 `${VAR}` 不能填入 int 字段
- 🟡 **EQL 边界静默失败**：`del(payload)` 不生效（`eql.go:284-301`）；filter 返回非 bool 静默当 false（`eql.go:155-168`）
- 🟡 **同 step 多 block 展开缺陷**：`normalize.go:152-215` `source+transform` 共存产生重复 stage id
- 🟡 **filter/map/route 三处 `_ = json.Unmarshal` 同一坏味道**；map transform 在 raw 路径下修改丢失（`map.go:51,71-72`）
- 🟡 **kafka sink Flush 为 no-op**（`kafka.go:86`）；**json/raw codec 配置完全忽略**

## 测试与工程化

- 🔴 **大量核心包与插件零测试**：`basestage`/`codec`/`message`/`registry`/`stage`、codec json/raw、sink 全部、httpserver、filter/map/wasm
- 🟡 测试多为 happy path：EQL 无错误路径用例、WAL 无崩溃/截断恢复测试、reload 测试重载同一配置
- 🟡 `time.Sleep` 轮询有 flaky 风险（`engine_integration_test.go:65`、`reload_test.go:51`）
- 🟡 Makefile 在 Windows 下找不到 `bin/edgestream.exe`，`make test` 未启用 `-race`
- 🔵 CLI 顶层 `--help` 退出码为 1；`skills/edgestream/reference.md` 与主文档有偏差

## 总体评价

架构骨架（per-edge 缓冲、聚合 Ack、分阶段 Stop）合理，接口层是合格的深模块，文档自省（design-review 过时标注）少见。主要问题：**可靠性语义是纸面承诺**（COW 被插件绕过、边级 DLQ/超时未落地、WAL 投递语义实际 at-most-once），**文档口径超前或滞后于代码**，**静默失败遍布配置、EQL、插件三层**。

## 优先级建议

1. **修消息语义**：Ack 幂等化；WAL offset 改为下游 Ack 后提交 + 记录加 CRC + offset 原子落盘；drain 用独立超时 ctx；map/route 调 `EnsureWritable()`
2. **统一配置严格性**：`KnownFields(true)`、HOCON 类型校验、修复示例 08
3. **对齐文档口径**：README 砍掉未实现能力声明，设计 §12/§13 更新完成度
4. **补关键路径测试**：插件行为、WAL 异常恢复、EQL 错误路径，`make test` 加 `-race`
